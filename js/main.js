// const listItem = document.querySelector('.list');

// listItem.addEventListener('focusin', handleFocus);
// // listItem.addEventListener('focusout', handleFocusOut);
// listItem.addEventListener('mouseover', handleFocus);

// function handleFocus(event) {
//     if (event.target.querySelector('.products')) {
//         const target = event.target.querySelector('.products');
//         if (target.classList.contains('visually-hidden')) {
//             target.classList.remove('visually-hidden');
//         }
//         target.addEventListener('mouseout', handleFocusOut);
//         return
//     }
//     return
// };

// function handleFocusOut(event) {
//     const target = event.currentTarget;
//     console.log(target)
//     target.classList.add('visually-hidden');
//     return
// }

// // if (!target.classList.contains('visually-hidden')) {
// //         target.classList.add('visually-hidden');
// //     }